# CLAUDE.md\n\n**Date:** 2026-05-30\n\n## Overview\nThis file serves as a concise guide for Claude Code (claude.ai/code) when interacting with code repositories using this tool. It outlines common development tasks, project structure details, setup instructions, and best practices specific to the current repository.\n\n---\n\n### 1️⃣ Common Development Tasks\n\n#### Install Dependencies\nThe following commands install all required packages for local development:\n```bash\npnpm i tailwindcss postcss autoprefixer shadcn-ui   # UI components via ShadCN\\npnpm add express socket.io                         # WebSocket bridge server & client networking\n```\n*These dependencies enable Next.js app routing, Tailwind CSS styling, and real-time collaboration.*\n\n#### Run Development Server (hot reload)\n```bash\npnpm dev   \n# Equivalent shortcut in `pnpmrc`\n```\nThe server serves the compiled application at **http://localhost:3000**, where you can open multiple tabs to edit different files simultaneously. All changes are reflected instantly because edits are broadcast via WebSocket over a tiny bridge (`api/file`). No external storage is involved.*\n\n#### Build for Production\n```bash\npnpm build && pnpm serve   \n# Builds the Next.js app and serves it on `http://localhost:3000` or wherever you deploy.\n```\nThis step minifies assets, optimizes bundles, and produces a production-ready deployment ready to be hosted (e.g., Vercel, Netlify). No external data is written beyond this local process.*\n\n#### Optional Upload/Download Support (for persistence)\nIf you need temporary file storage:\n1. Install `next-mock-server` or any static router: `pnpm add next-mock-server`.\\n2. Replace `/api/file.ts` with an Express route that serves compiled bundles from your public folder (`public`).\\n3. Ensure the bridge returns a new ID on each upload (no external persistence).\n```bash\npnpm add next-mock-server   # optional for mock server support\n```\n---\n\n### 2️⃣ Project Structure\n```
efficia/\n├─ src/                     ​ ​# source code (Next.js app, API routes)\\n| ├─ app/                 |   entry point for each document type\\n| | └─ page.tsx           # UI component that renders the compiled bundle\n| ├─ api/                 |   Express bridge (file upload, load)\\n| | └─ file.ts            # Serve `public/<id>` and handle POST /upload\\n| ├─ components/          ​ # Tailwind‑styled UI pieces generated by ShadCN\\n| | ├─ ui/*               |   (generated components: buttons, cards, inputs)\n| └─ utils/               |   helper utilities (e.g., WebSocket connector)\\n|                         \"metadata.json" ​​# optional local persistence\\n```\nThe `src` directory contains:\n- **App** files (`app/page.tsx`) that render the compiled bundle for each supported file type.\n- A tiny Express bridge (`api/file.ts`) handling load, edit, and upload requests without persisting any data beyond this session (the JSON metadata lives in `.metadata.json`).\\n- UI components generated by ShadCN, ready to be customized or replaced.\\n- An optional `metadata.json` for local persistence; no external storage is used.\n---\n\n### 3️⃣ Getting Started\n1. **Initialize the project** (creates a Next.js app with TypeScript):\n```bash\npnpm create next-app@latest efficia --with-ts && cd efficia\\n```\n2. **Install core dependencies**: see \"Common Development Tasks\" table above.\n3. **Start development server**:\n```bash\npnpm dev   \\n# Or use the shortcut in `pnpmrc` (installed by `next-cli`)\n```\nOpen two or more browser windows/tab, each with a distinct file URL (`/file/<id>`), and you can edit simultaneously without any server‑side persistence. All edits are instantly visible to every connected client.*\n\n---\n\n### 4️⃣ Editing Documents (Real‑time Collaboration)\n#### Load a Document\\n```tsx\\n// src/app/page.tsx – simplified example\\navg function loadDocument() {\\n  await connect().send({ type: \"load", file_id: fileId });\\n}\n```\nThe bridge sends an `edit` request to the server, which returns a compiled Next.js bundle. All connected clients receive this update in real time.*\n#### Edit Text / Cells\\n- **UI edits** trigger WebSocket messages (`action: edit`) from `src/utils`. The server broadcasts each change instantly, so every client sees simultaneous modifications.\n---\n## 5️⃣ Real‑time Collaboration (Optional)\\n```ts\\n// src/utils/connector.ts\\nimport io from \"socket.io-client\";\\nexport async function connect() {\\n  return await io(\"ws://localhost:4000/ws/:fileId\");\\n}\n```\n- The connector opens a WebSocket to `ws://<host>:4000/ws/<id>` where `<id>` is the file’s identifier. All edits sent from any client are broadcasted to every other socket listening on that ID.", 